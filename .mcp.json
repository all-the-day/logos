{
  "mcpServers": {
    "ssh-mcp-server": {
      "command": "npx",
      "args": [
        "-y",
        "@fangjunjie/ssh-mcp-server",
        "--host", "124.222.74.115",
        "--port", "22",
        "--username", "root",
        "--password", "duoban@0118",
        "--blacklist", "^rm -rf .*,^shutdown.*,^reboot.*,^dd .*,^mkfs.*"
      ]
    },
    "Pixso MCP": {
      "url": "http://localhost:3667/mcp"
    }
  }
}
